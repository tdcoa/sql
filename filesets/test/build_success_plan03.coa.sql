/*  added database setting in order to get special command to run: */
/*  adding an arbitrary RETURN SET to trigger special command */

/*{{pptx:success_plan.pptx}}*/
select 'minor bug, this special command requires record return'
;

/* this works, but unfortunately bug in pptx still exists */
