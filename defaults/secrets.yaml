# this file contains sensitive information that
# is typically NOT SHARED beyond one user, for
# example, passwords, some usernames, and IPs.

secrets:

  # TD QLID
  td_username: "myQLID"
  td_password: "myPassword"

  # customer credentials
  customer_username: "myCustUserName"
  customer_password: "myCustPassword"

  # altan's VDB (for testing)
  avdb_username:   "vantagelabuser"
  avdb_password:   "vantagelabpwd"
